# Papertiger 0.9.0 release readiness

Date: 2026-08-23
Host: Windows, Europe/Amsterdam
Authority-audit checkpoint: `7569c107736f6f0cf0ba924b61f05a91f841b367`
Binding-migration and fresh-dogfood source checkpoint:
`2dec4e8b5ba8ea6b7e420132acdeb8baa6b66c07`

## Verdict

Papertiger 0.9.0 is ready for the operator-owned publication workflow. The
release-critical code, schema migration, refusal contracts, installation
surface, agent instructions, public changelog, exact workspace-built artifact,
and local release workflow pass their gates. The complete authority-wide
evidence surface now verifies, rather than only the release-scoped subset.

No tag, release asset, branch update, workflow dispatch, or publication was
performed. Readiness is the boundary of this tranche; publication remains an
explicit operator action.

## Frozen release artifacts

| Artifact | Version | Bytes | SHA-256 |
| --- | --- | ---: | --- |
| `target/release/papertiger.exe` | `papertiger 0.9.0` | 4,883,968 | `3c29a3eccab2e1d8bd1c23480cb2070009d90a3bdbf2e0ff613d15ea656e324c` |
| `target/release/papertiger-mise.exe` | `papertiger-mise 0.9.0` | 9,070,080 | `2467e8d82d880426c77e876c77d34228f45a913963ffddef8f3f2999776c3979` |

The release command is
`cargo build --locked --release --workspace --bins`. After that command and all
build gates completed, the planner was frozen, installed into this repository,
and measured in fresh dogfood fixtures. Subsequent cached workspace builds after
the evidence-only commits retained the exact planner and Mise byte identities.
The self-hosted planner is byte-identical to the frozen artifact.

An adversarial check found that package-only and workspace builds do not have
the same planner size: the workspace graph also enables Clap's `env` feature
through Mise. Repeated Windows relinks also change PE hashes while preserving
source and size. This record therefore binds one frozen workspace artifact; it
does not claim byte-for-byte reproducible Windows builds.

## Required gates

| Gate | Result |
| --- | --- |
| `cargo fmt --all -- --check` | pass |
| `cargo clippy --workspace --all-targets` | pass, zero warnings |
| `cargo test --workspace` | pass: 260 tests, zero failures, 2 intentional subprocess helpers ignored |
| `cargo test -p papertiger-mise --test deterministic_dogfood -- --test-threads=1` | pass: 2/2 in 105.73 s |
| Git-for-Windows `bash scripts/cross_check.sh` | pass |
| changelog-writing skill structural validation | pass |
| release-note rendering | pass: 3,293 bytes, SHA-256 `882cd6d474ee073004de5806e488c6bf6ad4c0731d62db670fe17079e57284a7` |
| release dispatch validation on `refs/heads/master` | pass with publication disabled and enabled |
| self-hosted `setup-project --dry-run --json` | `operation=unchanged` for every managed surface |
| authority-wide evidence verification | 81 bindings verified, zero failed, zero unverifiable |
| installed `papertiger audit` | `no findings` |
| Git worktree | clean at the dogfood and pre-closure export checkpoints |

The workspace total comprises 8 planner library, 33 planner binary, 17 planner
CLI, 70 planner core, 122 Mise library, 2 deterministic dogfood, 5 lineage CLI,
and 3 project-root CLI tests. The two ignored Mise cases are internal
subprocess helpers exercised by their parent process tests, not disabled
product coverage.

The passing cross-check repeated locked formatting, warning-denied clippy,
workspace tests, and the serialized deterministic campaign before building
both release binaries. It also proved:

- planner/Mise version alignment at 0.9.0;
- version-mismatch and non-master publication refusals name corrective choices;
- release-note rendering preserves semantic paragraphs and refuses a missing
  changelog section;
- a fresh consumer receives the native planner, tracked receipt, canonical
  contract, and byte-identical skill envelopes;
- nested native and PowerShell reads discover one authority without changing
  its bytes;
- the consumer planner initializes schema v8 and audits cleanly;
- the independent Mise authority initializes without being mistaken for the
  planner authority; and
- a second setup is idempotent.

An unqualified `bash` attempt resolved to WSL, whose VM creation failed with
host error `0x800705aa`. A Cygwin attempt then reached native setup but could
not translate its `/tmp` path for the Windows executable. Neither is counted as
a product result. The intended Git-for-Windows/MSYS run passed the complete
script and its native-path boundary.

## Exact-artifact dogfood

The frozen planner passed two complete 8/8 matrices across Codex CLI, Claude
Code with Claude Fable, Pi with GPT-5.6 Luna, and Pi with DeepSeek V4 Flash.
The second matrix ran after the authority-wide evidence migration:

- every routing harness produced two done user-sourced tasks, two resolving
  full-OID associations, two focused commits, a clean worktree, and a clean
  audit;
- the bounded-edit and read-only controls produced zero tasklog events and
  their exact expected repository state; and
- both domain-owned lifecycle controls produced zero tasklog events and one
  campaign-only commit.

The fresh authority-closure record binds every raw stream, final response,
task, association, commit object, prompt, and limitation. Its complete archive
is
`F:\AI\papertiger-authority-backups\papertiger-0.9.0-authority-closure-dogfood-20260823.zip`:
216,883,785 bytes, SHA-256
`07cf050719573fffe26a17d536887ae935894c8c2822a0c7f0637f10676335e8`.
The canonical receipt is 19,098 bytes with SHA-256
`eb3ed8958930492a4a16a2d8018e60466320ca5fd59da675856aa4f44244f203`.

Three earlier matrices remain retained separately. Their DeepSeek sequence was
no route, an incomplete route, then no route before the frozen artifact's full
route. The final 8/8 result is exact artifact behavior, not a universal or
statistical model-reliability claim.

## Instruction and naming closure

Always-loaded repository guidance now gives an executable order: qualifying
work reads `.agents/skills/papertiger/SKILL.md` completely before the first edit
or commit. Known user intent defaults to `intent_source=user`, and every
commit-backed outcome receives an inward full-OID association before task
completion.

Current vocabulary is functional and compact: **repository guidance discovery
trigger**, **implicit tasklog routing**, **content provenance**, **authority
identity**, and **Mise integrity boundaries**. The subjective
`intuitive-adoption` label and live metaphorical `trip-wire` terminology are
gone. Historical passages retain obsolete terms only when quoting or
invalidating earlier evidence.

`AGENTS.md` and `CLAUDE.md` are byte-identical at 6,925 bytes with SHA-256
`9f466122a684d90f0330f635971a146c47e899e2a13508b5bc3ef9d6f30104bb`,
well below the 32 KiB limit. Source and installed operating contracts are
byte-identical at 16,397 bytes with SHA-256
`ed89760fd9e07dadf5a724b6ec7e3b80527f69d04c47fc1d2e2e93b66fb2e771`.
The three Papertiger skill envelopes are byte-identical at 3,030 bytes with
SHA-256
`32c247b0274045acc5feec0d0360b5e11a2cec2385d8d14defc559e23aa3ecef`.

## Human changelog closure

The repository-local `.agents/skills/changelog-writing/SKILL.md` is 5,013 bytes
with SHA-256
`57a21bdbf0d69cf7b6986bb9c157500f726c3d959a42a746710dc212d11fe4a6`;
the skill-creator validator passes. Its source-first workflow was applied to
the 0.9.0 changelog.

The resulting `CHANGELOG.md` is 12,290 bytes with SHA-256
`37b4f0ec83db9c16364492ad3db4f617275d1b71dbecca8617fae4654dce3914`.
It describes user-visible capabilities, behavior changes, migration and
compatibility boundaries, exact public names, and corrective commands without
dogfood ceremony, internal task state, or test-count marketing. The release
renderer and both master-branch dispatch modes accept it.

## Evidence-verifier closure

The initial authority-wide report contained 73 nonverified historical bindings:
38 stale, missing, unhashed, or external `file:` bindings and 35 schemes the
local verifier does not resolve. Every original locator, digest, event, note,
result, and commit association is preserved in the immutable audit receipt and
the frozen pre-migration export. Independent recovery found exact historical
bytes for 20 of the 38 file defects; 18 retain explicit identity-loss
limitations. Nine of eleven Mise records fully reopened; two retained
Contextmink nominations preserve the source-relocation rederivation limit.

All 73 live gates were then rebound append-only to the immutable audit receipt,
never to unrelated current bytes. The migration temporarily opened the required
45-task dependency and hierarchy closure and six historical plans, then restored
every status, result and provenance, hierarchy, replacement, association, and
plan disposition with zero comparison differences.

The current authority-wide report is complete: 81 bindings verify, with zero
failed and zero unverifiable. `papertiger audit` reports no findings. The audit
and migration records are:

- `docs/evidence/2026-08-23-historical-evidence-binding-audit.md`
- `docs/evidence/2026-08-23-historical-evidence-binding-migration.md`

## Authority recovery export

Immediately before the append-only binding migration, the live authority was
exported through the public API to
`F:\AI\papertiger-authority-backups\papertiger-0.9.0-pre-historical-binding-migration-20260823.json`.
The `papertiger.dump.v7` export contains 9 plans, 138 tasks, 1,112 events, and
1 Mise projection. It is 1,263,050 bytes with SHA-256
`501dd46320644556d9a0becf9e4badabcbb4771e8d9a2bc57438e574d628d2bd`.

The final post-closure recovery export is intentionally recorded after the
release task closes, so its digest belongs in the append-only tasklog rather
than inside this gate document. Neither export is a second live authority; the
receipt-selected database remains canonical.

## Remaining boundary

The repository and its two planning authorities contain non-release work that
is not a dependency of 0.9.0 and is not represented as complete here. Local
readiness also does not substitute for the GitHub-hosted multi-platform
packaging workflow or its operator-controlled publication step.
